import storageClientFactory, {
  StorageClient,
  MemoryStorage,
  StorageCore,
  IStorageInitConfig,
  IStorageInterface,
  IStorageHandleOptions,
  IMemoryStorageHandleOptions
} from './storageClient.imp';

export default storageClientFactory;

export {
  StorageClient,
  MemoryStorage,
  StorageCore,
  IStorageInitConfig,
  IStorageInterface,
  IStorageHandleOptions,
  IMemoryStorageHandleOptions
};
